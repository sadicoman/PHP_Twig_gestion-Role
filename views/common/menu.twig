<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link" aria-current="page" href="{{ constant('URL') }}accueil">Accueil</a>
        </li>
        {% if not is_connected %}
          <li class="nav-item">
            <a class="nav-link" aria-current="page" href="{{ constant('URL') }}login">Se connecter</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" aria-current="page" href="{{ constant('URL') }}creerCompte">Créer compte</a>
          </li> 
        {% else %}
          <li class="nav-item">
            <a class="nav-link" aria-current="page" href="{{ constant('URL') }}compte/profil">Profil</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" aria-current="page" href="{{ constant('URL') }}compte/deconnexion">Se déconnecter</a>
          </li>
        {% endif %}
        {% if is_connected and is_admin %}
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Administration
            </a>
            <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
              <li><a class="dropdown-item" href="{{ constant('URL') }}administration/droits">Gérer les droits</a></li>
            </ul>
          </li>
        {% endif %}
      </ul>
    </div>
  </div>
</nav>
